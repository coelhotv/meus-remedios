# Sprint 2026-W12 — Bugfix Console Errors + Docs Sync

**Data:** 2026-03-18
**PR:** #401 — mergeado em main

---

## Entregas

### Fix: 3 erros no console desktop/dev (PR #401)

**Erro 1 — `data:text/jsx;base64,...` MIME type error**
- **Root cause:** `index.html` tinha hint manual `<link rel="modulepreload" href="/src/main.jsx" />`. Vite 7 base64-encoda o JSX raw em vez de resolver para o bundle compilado. O dist/index.html ficava com `data:text/jsx;base64,...` que o browser rejeita.
- **Fix:** Remoção do hint manual. Vite gera os hints corretos automaticamente.
- **Anti-pattern registrado:** AP-B01

**Erros 2 e 3 — `column medicine_logs.status does not exist` + `column medicine_logs.treatment_plan_id does not exist`**
- **Root cause:** `getAllPaginatedSlim` em `logService.js` selecionava colunas inexistentes na tabela. Schema real: `id, protocol_id, medicine_id, taken_at, quantity_taken, notes, user_id`.
- **Fix:** Removidos `status` e `treatment_plan_id` da query SELECT e do JSDoc. Removido `status` da comparação de memo em `LogEntry.jsx`.
- **Anti-pattern registrado:** AP-B02

### Docs: Sync DATABASE.md + SCHEMAS.md com schema real

- `DATABASE.md`: adicionada `therapeutic_class` em medicines, nota sobre `DEFAULT 'medicine'` vs CHECK constraint, default `'enviada'` em notification_log, nota sobre titration_status sem CHECK
- `SCHEMAS.md` (v2.0): reescrita completa — paths corretos (`src/schemas/`), schemas atualizados (taken_at, DOSAGE_UNITS, MEDICINE_TYPES reais), aviso sobre colunas inexistentes em medicine_logs

---

## Lições Aprendidas

- **Vite 7 bug:** `<link rel="modulepreload" href="/src/*.jsx">` manual em index.html → Vite 7 base64-encoda o JSX em vez de resolver → não adicionar hints manuais de modulepreload
- **Schema drift:** `medicine_logs` não tem `status` nem `treatment_plan_id`. Esses campos existem em outras tabelas (`notification_log.status`, `protocols.treatment_plan_id`). A documentação SCHEMAS.md estava ~1.5 anos desatualizada.
- **Ferramenta útil:** `vercel` CLI disponível localmente para checar deploys

### UX: Redução de ruído visual no StockCard (PR #402)

**Problema:** StockCard exibia todas as entradas de estoque com o mesmo peso visual:
1. Lotes com `quantity = 0` (zerados por FIFO via `stockService.decrease()`)
2. Entradas geradas automaticamente por `logService.delete()` → `stockService.increase(id, qty, "Dose excluída (ID: X)")`

**Fix:** Classificação das entradas em 3 tipos antes de renderizar:
- **Lotes ativos**: `quantity > 0` e `notes` não é ajuste de sistema → exibidos normalmente
- **Lotes consumidos**: `quantity === 0` → ocultos por padrão (seção colapsável)
- **Ajustes automáticos**: `notes` começa com `"Dose excluída"` ou `"Ajuste de dose"` → ocultos por padrão

**Padrão aplicado:**
```javascript
const SYSTEM_NOTE_PREFIXES = ['Dose excluída', 'Ajuste de dose']
const isSystemAdjustment = (e) => SYSTEM_NOTE_PREFIXES.some(p => e.notes?.startsWith(p))
const { activeEntries, consumedEntries, systemEntries } = stockEntries.reduce(...)
```

**Correções adicionais do review Gemini:**
- `new Date()` → `parseLocalDate()` em formatDate/isExpired/isExpiringSoon (violação R-020)
- 3x `Array.filter()` → uma passagem com `reduce()` (performance)
- `||` → `Array.some()` + constante de módulo (extensibilidade)

---

## Lições Aprendidas

- **Vite 7 bug:** `<link rel="modulepreload" href="/src/*.jsx">` manual em index.html → Vite 7 base64-encoda o JSX em vez de resolver → não adicionar hints manuais de modulepreload
- **Schema drift:** `medicine_logs` não tem `status` nem `treatment_plan_id`. Esses campos existem em outras tabelas (`notification_log.status`, `protocols.treatment_plan_id`). A documentação SCHEMAS.md estava ~1.5 anos desatualizada.
- **Ferramenta útil:** `vercel` CLI disponível localmente para checar deploys
- **Stock entries são "sujas":** `stockService.increase()` gera entradas reais no DB quando uma dose é deletada. Qualquer UI que lista entradas de estoque deve classificá-las antes de exibir (ativas / consumidas / ajustes automáticos).

---

## Estado do Projeto

- v3.3.0 main — todos os 3 erros de console corrigidos + StockCard noise reduction
- DATABASE.md e SCHEMAS.md sincronizados com export SQL real do Supabase (2026-03-18)

---

# Sprint 8.3 — Chatbot IA Multi-Canal (F8.1)

**Data:** 2026-03-20
**Branch:** `feature/fase-8/sprint-3-chatbot`
**Status:** ✅ ENTREGUE E MERGEADO
**Commit:** `5a708ad` (squash)

---

## Entregas

### F8.1: Groq API + Context Builder + Safety Guard + Chat UI (13 SP)

**Arquivos criados:**
- `src/features/chatbot/services/contextBuilder.js` — Monta contexto compacto do paciente sem incluir IDs/UUIDs
- `src/features/chatbot/services/safetyGuard.js` — Filtra intenções perigosas (dosagem, parar tratamento, diagnóstico)
- `src/features/chatbot/services/chatbotService.js` — Rate limiting 30msg/hora (localStorage), integração com `/api/chatbot`
- `src/features/chatbot/components/ChatWindow.jsx` — Drawer lateral responsivo com animação Framer Motion, quick suggestions
- `api/chatbot.js` — Serverless function Groq (slot 7/12), `llama-3.3-70b-versatile`, max 300 tokens
- `src/features/chatbot/__tests__/contextBuilder.test.js` — 9 testes
- `src/features/chatbot/__tests__/safetyGuard.test.js` — 12 testes
- `src/features/chatbot/__tests__/chatbotService.test.js` — 7 testes

**Arquivos modificados:**
- `src/App.jsx` — Lazy import + FAB 💬 + ChatWindow integrado via Suspense
- `vercel.json` — Rewrite `/api/chatbot` adicionado ANTES do catch-all (R-040/AP-011)
- `package.json` — `groq-sdk` adicionado como dependência

**Quality gates:**
- 539/539 testes passaram ✅
- Build: ChatWindow chunk = 3.23 kB gzip ✅
- Budget serverless: 7/12 ✅
- contextBuilder nunca inclui IDs/UUIDs ✅
- safetyGuard bloqueia dosagem/parar/substituição/diagnóstico ✅
- Disclaimer médico em toda resposta com keywords de saúde ✅

---

## Decisões de Design

1. **ChatWindow chama `useDashboard()` internamente** (em vez de receber `patientData` via prop de App.jsx). App.jsx não está dentro do DashboardProvider — só os filhos são. Esta abordagem é mais limpa e evita prop drilling.

2. **`contextBuilder.js` adaptado para DashboardContext** (spec usava `{ stocks }` raw, mas DashboardContext fornece `stockSummary` e `medicines[i].stock` embedded). Adaptado para usar `stockSummary` ou fallback para `medicine.stock`.

3. **Groq SDK instanciado dentro do handler** (não no topo do módulo) para evitar erro quando `GROQ_API_KEY` está indefinida no startup.

---

## Review Gemini Code Assist (2026-03-20 11:06)

Gemini identificou 5 sugestões (2 HIGH, 3 MEDIUM). Todas foram aplicadas e aceitas:

| # | Prioridade | Sugestão | Fix Aplicado | Commit |
|----|-----------|----------|------------|--------|
| 1 | HIGH | Validação manual em api/chatbot.js | Zod `chatbotRequestSchema` com `safeParse()` | `7644217` |
| 2 | HIGH | Inline styles em ChatWindow.jsx | `ChatWindow.module.css` (14 classes) | `0aaf269` |
| 3 | MEDIUM | FAB inline styles em App.jsx | `App.module.css` (.chatFab) | `0aaf269` |
| 4 | MEDIUM | Catch vazio em chatbotService.js | `console.error('[chatbot]', error)` | `6db4226` |
| 5 | MEDIUM | `toDateString()` sensível a timezone | Comparação por `getFullYear/Month/Date()` (R-020) | `6db4226` |

**Workflow:** Submit PR → Gemini review → 5 inline replies → Gemini re-review (0 novos comentários) → Merge com squash

---

## Lições Aprendidas

- **DashboardProvider wrap vs children:** App.jsx é o *wrapper* do DashboardProvider, não um filho. Componentes renderizados DENTRO do JSX do App (como ChatWindow) SÃO filhos e PODEM chamar `useDashboard()`.
- **Spec pode ter dados desacoplados do contexto real:** spec mostrava `patientData = { stocks }` mas o DashboardContext não expõe stocks brutos — apenas `stockSummary`. Sempre verificar o retorno real do hook antes de implementar.
- **Groq SDK:** instanciar na call do handler (não no módulo) evita crash quando env var ausente.
- **CSS Modules já existem no projeto:** Views (Calendar, Settings, Dashboard) usam `.module.css`. Refatorar componentes novo é baixo risco, melhora manutenibilidade.
- **Todas as 5 sugestões Gemini foram HIGH ou MEDIUM:** Demonstra qualidade da implementação inicial — nenhuma sugestão LOW ou CRITICAL/negligência.

---

# Sprint 6.3 — Integrações Cross-Cutting (INT-01 + INT-02)

**Data:** 2026-03-20
**PR:** #406 — mergeado em main
**Status:** ENTREGUE ✅

---

## Entregas

### INT-01: Risk Score no PDF Reports (2 SP)

- `pdfGeneratorService.js`: nova função `drawRiskTable()` que exibe tabela "Risco por Protocolo" (Protocolo | Adesão 14d | Tendência | Classificação) com cores por nível de risco
- `generatePDF()` agora aceita `includeRiskTable=true` (default), lazy-loads `protocolRiskService.js` e `logService.js`, busca logs dos últimos 30 dias via `logService.getByDateRange()`, e chama `calculateAllProtocolRisks()` para os protocolos com dados suficientes
- Feature flag automático: só renderiza a tabela se `hasEnoughData === true` em algum protocolo

### INT-02: Refill Prediction nos Alertas Bot (3 SP)

- `tasks.js`: nova função `calcBotRealDailyConsumption()` — duplica lógica do refillPredictionService para uso server-side (Option A da spec)
- `tasks.js`: nova função `formatStockoutDate()` — formata data prevista como "DD/MM"
- `checkUserStockAlerts()`: agora fetcha `medicine_logs` dos últimos 30 dias em paralelo via Promise.all, calcula consumo real por medicamento, adiciona `predictedStockoutDate` aos objetos de alerta
- `formatStockAlertMessage()` e `formatProactiveStockMessage()`: exibem "(previsto: DD/MM)" quando data disponível

### Bugfix: reminderOptimizerService localStorage (Sprint 6.4 pre-existing)

- `reminderOptimizerService.test.js`: adicionado mock localStorage com `vi.stubGlobal()` (AP-T03)
- `reminderOptimizerService.js`: guard clause aprimorado para localStorage indisponível em testes
- `proactiveStockAlerts.test.js`: mock do Supabase atualizado para suportar `.gte()` na tabela `medicine_logs`

---

## Lições Aprendidas

- **Mock Supabase por tabela precisa cobrir TODOS os métodos chamados:** quando adicionamos query a nova tabela (`medicine_logs` com `.gte()`), mocks de teste antigos quebram. Sempre revisar os mocks ao adicionar queries.
- **localStorage em jsdom pode não funcionar:** mesmo com `environment: 'jsdom'`, se `--localstorage-file` tiver path inválido, localStorage não está disponível. Usar `vi.stubGlobal('localStorage', mockObj)` é mais confiável.
- **Option A para bot (duplicar lógica):** refillPredictionService é client-side mas bot é server-side. Duplicar a lógica de consumo real no bot é mais limpo que compartilhar código entre client/server com path aliases diferentes.
- **toISOString() viola R-020 mesmo indiretamente:** usar `new Date().toISOString().slice(0,10)` para gerar string de data produz data UTC, não local. Em GMT-3 após as 21h, retorna o dia seguinte. Usar sempre `formatLocalDate(new Date())`.
- **Gemini aceita justificativas de design quando bem documentadas:** ao recusar sugestão de refatoração, citar CLAUDE.md + spec + journal foi suficiente para o revisor concordar sem insistir.

---

# Sprint 8.3.1 — Bugfix Chatbot Hallucinations

**Data:** 2026-03-20 (pós-deploy)
**PR:** #408 — mergeado em main
**Status:** ENTREGUE ✅
**Commit:** `1e47cfb`

---

## Problema

Chatbot alucinava respostas farmacológicas — quando perguntado "qual a função do Selozok?", o LLM respondeu "Sertralina (antidepressivo)" em vez de "Metoprolol Succinato (betabloqueador)". Erro perigoso num contexto médico.

## Análise das Causas

1. **Falta de grounding farmacológico:** contextBuilder enviava só `SeloZok (50mg)` sem `active_ingredient` ou `therapeutic_class`. LLM tinha que recorrer ao conhecimento de treinamento — onde errou.
2. **Parâmetros de inferência muito altos:** `temperature: 0.7, top_p: 0.9` faziam o modelo amostrar de distribuição ampla, aumentando chance de "inventar" quando contexto incompleto.
3. **Modelo base:** `llama-3.3-70b-versatile` tem reputação moderada em factual recall (não é o melhor para farmacologia).

## Solução (Opções E + Ajustes Parametros + D)

### 1. Grounding com Contexto Farmacológico (Opção E)
- Adicionar `active_ingredient` e `therapeutic_class` ao contexto do paciente
- **Antes:** `- SeloZok (50mg): diario, ...`
- **Depois:** `- SeloZok [Succinato de Metoprolol, Betabloqueador] (50mg): diario, ...`
- Garantir `null` ou string vazia não expõe texto "null" no contexto (usar `filter(Boolean)`)

### 2. Ajuste de Parâmetros (Temperature + Top_p)
- `temperature: 0.7 → 0.2` — respostas factuais, menos aleatoriedade
- `top_p: 0.9 → 1.0` — desativar nucleus sampling quando temperatura baixa
- Resultado: modelo menos propenso a inventar

### 3. Trocar Modelo (Opção D)
- Trocar para `groq/compound` (seleção inteligente do modelo conforme "pedido")
- Reduz dependência de modelo específico com baixo factual recall

## Arquivos Modificados

| Arquivo | Mudança |
|---------|---------|
| `api/chatbot.js` | `temperature: 0.2, top_p: 1.0` (linhas 57-58) |
| `src/features/chatbot/services/contextBuilder.js` | Incluir `principioAtivo` + `classeTerapeutica`; filtrar Boolean para omitir null |
| `src/features/chatbot/__tests__/contextBuilder.test.js` | Adicionar testes para `therapeutic_class` + ausência de null |
| `src/features/chatbot/__tests__/chatbotService.test.js` | Corrigir mock localStorage com `vi.stubGlobal()` (AP-T03) |

## Quality Gates

- ✅ 33/33 testes passando
- ✅ Nenhum `null` ou `undefined` exposto no contexto
- ✅ localStorage mock corrigido (AP-T03)
- ✅ Sem impacto em UX — mudanças internas apenas

## Lições Aprendidas

- **Alucinação = falta de grounding, não só prompt:** restricoes no prompt sozinhas não fazem o LLM deixar de inventar farmacologia. Precisa de contexto factual + parametros conservadores.
- **Dados do BD já existem:** `active_ingredient` + `therapeutic_class` já estavam nas medicines — só não estavam sendo enviados ao LLM.
- **Temperature baixa é crítico para medical chatbots:** `0.2` vs `0.7` muda drasticamente a disposição do modelo em "arriscar" respostas.
- **Modelo importa:** mesmo com tudo acima, qualidade do modelo base impacta (por isso opção D — groq/compound).

---

## Sprint 8.3.2 — Chatbot IA no Telegram

**Data:** 2026-03-20 (mesmo dia que 8.3.1)
**PR:** #409 | **Commit:** `bc59836`
**Contexto:** Testado em produção → resposta correta ("SeloZok = Metoprolol Succinato") → estender para Telegram imediatamente.

### Decisões de Arquitetura

**Não reusar chatbotService.js do cliente.** O web service usa `localStorage` (browser-only) e `fetch('/api/chatbot')` (URL relativa). Para o servidor Node.js, é mais limpo ter um serviço dedicado.

**Groq SDK diretamente, não via Vercel endpoint.** Evita hop de rede adicional, simplifica debugging e o server já tem acesso à GROQ_API_KEY.

**Rate limit via Map em memória.** Simples e suficiente para Telegram (volume muito menor que web). Reinicia com o processo — aceitável.

**Graceful degradation silenciosa** (sem GROQ_API_KEY ou usuário não vinculado): não enviar mensagem de erro — o usuário pode não saber que o chatbot existe.

### Novos Arquivos

| Arquivo | Responsabilidade |
|---------|-----------------|
| `server/bot/services/chatbotServerService.js` | Serviço completo: fetch Supabase, contexto, Groq, rate limit, histórico |
| `server/bot/commands/chatbot.js` | Handler `bot.on('message')` para textos não-comando |

### Tabela de inconsistência resolvida

`stock_entries` (nomenclatura intuitiva) ≠ `stock` (nome real da tabela). Erro detectado comparando com outros comandos do bot.

### Quality Gates
- ✅ 539/539 testes (sem regressão)
- ✅ Lint: zero erros
- ✅ Contexto anti-alucinação idêntico ao web (active_ingredient + therapeutic_class)

---

## Sprint 8.4 — Chat History Persistence

**Data:** 2026-03-20 (pós-merge Sprint 8.3.2)
**PR:** #410 | **Branch:** `feature/chatbot-history-persistence`
**Status:** ENTREGUE ✅
**Commits:**
- `f1cf63b` — feat(chatbot): add history persistence configuration constants
- `efb9b88` — feat(chatbot): add localStorage persistence functions for chat history
- `989e721` — feat(chatbot): implement history persistence UI with timestamps and date separators
- `72298b9` — refactor(chatbot): centralize welcome message and extract helper functions

### Problema Resolvido

Usuario perdia histórico de conversas ao fechar/reabrir o painel do chatbot. Cada abertura mostrava apenas a mensagem de boas-vindas, forçando repetição de perguntas.

### Solução Implementada

#### 1. Configuração Centralizada (`chatbotConfig.js`)

Movido `createWelcomeMessage()` para `chatbotConfig.js` para reutilização (web + Telegram futuro).

```javascript
export const CHATBOT_HISTORY_STORAGE_KEY = 'mr_chat_history'
export const CHATBOT_HISTORY_MAX_DISPLAY = 20  // 10 turnos
export function createWelcomeMessage() { ... }
```

#### 2. Persistência (`chatbotService.js`)

Três funções novas:

```javascript
export function loadPersistedHistory()         // → carrega localStorage
export function savePersistedHistory(messages) // → salva com limite
export function clearPersistedHistory()        // → limpa ao usuário pedir
```

Limite: máx 20 mensagens (10 turnos), descartando as mais antigas. Backward compat: histórico antigo sem timestamp é ignorado.

#### 3. UI (`ChatWindow.jsx`)

**Lazy init:**
```javascript
const [messages, setMessages] = useState(() => {
  const persisted = loadPersistedHistory()
  if (persisted.length > 0) return persisted
  return [createWelcomeMessage()]
})
```

**Timestamps:** Cada mensagem agora tem `timestamp: Date.now()`.

**Exibição relativa:**
- Hoje: "às 14:30"
- Ontem: "Ontem às 09:15"
- Antiga: "15/03 às 17:42"

**Separadores de data:** Quando usuário pula para dia anterior → exibe label ("Hoje", "Ontem", "15/03") com linhas decorativas.

**Botão "Limpar conversa"** (🗑️): Com confirmação dialog, reseta a localStorage e volta para mensagem de boas-vindas.

**Refatoração (feedback Gemini):**
- Extraído `addMessage()` helper → elimina duplicação (user + assistant)
- Movidas funções puras para fora do componente → performance (não recreadas a cada render)
  - `formatMessageTime()`
  - `shouldShowDateSeparator()`
  - `formatDaySeparator()`
- **Fix DST:** Substituído `new Date(now - 86400000)` por `yesterday.setDate(now.getDate() - 1)` para lidar corretamente com transição de horário de verão.

#### 4. Estilos (`ChatWindow.module.css`)

Adicionadas classes:
```css
.messageTime      /* timestamp abaixo de cada bolha */
.dateSeparator    /* label com linhas decorativas */
.headerActions    /* flex container para botões */
.clearButton      /* trash icon com hover */
```

### Review Gemini (5 sugestões, todas applied)

| # | Prioridade | Sugestão | Applied |
|----|-----------|----------|---------|
| 1 | Medium | Welcome message duplication | Movido para `chatbotConfig.js` |
| 2 | Medium | Add message logic duplication | Extraído `addMessage()` helper |
| 3 | Medium | Helper functions recreated on render | Movidas para fora do componente |
| 4 | Medium | DST handling with fixed milliseconds | Fix com `setDate()` |
| 5 | Medium | Unreachable code check in `savePersistedHistory` | Removido |

Gemini respondeu positivamente a cada fix ("Excelente!", "Ótimo!", "Agradeço a confirmação...").

### Quality Gates

- ✅ 539/539 testes passando
- ✅ Lint: zero erros
- ✅ Bundle: ChatWindow chunk ainda 3.23 kB gzip (localStorage é native, zero overhead)
- ✅ UX: Histórico recuperado em reload, navegação entre views, mobile OK
- ✅ Backward compat: Histórico antigo sem timestamp descartado graciosamente

### Lições Aprendidas

- **Centralize configuração compartilhável:** `createWelcomeMessage()` em `chatbotConfig.js` é reutilizável para Telegram (e futuro Voice/SMS).
- **Pure functions fora de componentes:** React lazy-cria funções a cada render. Se puras (sem estado/props), extrair melhora performance e separação de responsabilidades.
- **DST é real:** `86400000` ms nem sempre = 1 dia (mudança de hora). `setDate(getDate() - 1)` é a abordagem robusta.
- **Timestamp relativo é UX melhor que absoluto:** "Ontem às 14:30" é mais intuitivo que "2026-03-19T14:30:00Z" para chat casual.
- **5 Medium → 0 Critical/High sugestões** indica qualidade da implementação inicial.

---
