# W14.5 — CRUD Usability Treatments (2026-04-02)

## Entregue

- **S14.5.1**: Link "todos medicamentos →" abaixo da busca ANVISA; topbar layout 2 colunas
- **S14.5.2**: `NewTreatmentDropdown` com 2 opções (simples) ou 3 (complexo); fix `refetchAll → refresh` em MedicinesRedesign; fix `frequency: 'diario' → 'diário'` em TreatmentWizard
- **S14.5.3**: Editar + deletar plano de tratamento; `TreatmentPlanForm` ganha campos `emoji` e `color` (picker nativo + hex)
- **S14.5.4**: Deletar tratamento — botão Trash2 em card (absoluto, sempre visível) e tabular (hover da linha)
- **S14.5.5**: `TreatmentPlanForm` redesenhado com tokens santuário (`form-row`, `form-label`, `error-message`)
- **S14.5.6**: Terminologia "protocolo" → "tratamento" em toda a UI do redesign; fix fluxo novo medicamento → novo tratamento via `initialMedicineId`; fix glow neon rosa em `ConfirmDialog` (btn-primary e btn-danger)

## Bugs corrigidos ao longo da wave

- `MedicinesRedesign`: `refetchAll` não existia no contexto — corrigido para `refresh`
- `TreatmentWizard`: `FREQUENCY_LABELS` local com chaves sem acento conflitava com schema Zod
- `Modal` renderiza via portal fora de `[data-redesign]` → estilos neon legados vazavam; fix via overrides em `ConfirmDialog.css`
- `navigateToProtocol` no redesign navegava para view legada `Protocols` — corrigido para `treatment` com `initialMedicineId`

## Lições

- Modais via portal ficam fora do `data-redesign="true"` — sempre adicionar overrides em `.sr-confirm-dialog` para qualquer botão dentro de ConfirmDialog
- `FREQUENCY_LABELS` e enums com acento: manter ÚNICA fonte (schema) para evitar divergência
- Fluxo cross-view (med → tratamento) precisa de estado no App + prop `initialMedicineId` para sobreviver à navegação
