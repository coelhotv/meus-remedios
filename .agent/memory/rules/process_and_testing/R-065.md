---
id: R-065
title: Read Memory Before Coding
summary: Every agent MUST load the relevant .agent/memory indexes before writing any code.
applies_to:
  - all
tags:
  - process
incident_count: 1
last_referenced: None
review_due: 2026-06-30
status: active
layer: hot
pack: review-validation
bootstrap_default: True
---

# R-065 — Read Memory Before Coding [CRITICAL]

## Rule
Every code agent MUST load the relevant `.agent/memory/` indexes BEFORE writing any code.

**Required sequence:**
1. Read `.agent/memory/rules.json` → note relevant R-NNN rules
2. Read `.agent/memory/anti-patterns.json` → note relevant AP-NNN patterns
3. Check for duplicate files (R-001)
4. Only then start coding

## Why
PR #132 review process. Agent repeated a known mistake (from AP-023) that was already documented. Reading memory first takes ~2 minutes and prevents repeating costly incidents.

## DEVFLOW note
In DEVFLOW this rule is enforced architecturally via the mandatory Bootstrap phase. This rule documents the intent for the current `.agent/memory/` context.
