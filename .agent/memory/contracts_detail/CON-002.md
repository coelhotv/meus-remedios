# CON-002: medicineService.create(medicine)

**File:** src/features/medications/services/medicineService.js

## Signature

```javascript
async create(data: MedicineInput): Promise<Medicine>
```

## Input Validation

Use `validateMedicineCreate()` from `src/schemas/medicineSchema.js`:

```javascript
const validation = validateMedicineCreate(newMedicine)
if (!validation.success) throw validation.errors
```

## Consumers

- `src/features/medications/components/MedicineForm.jsx`
- `src/views/Medicines.jsx`

## Breaking Changes

- Input field addition/removal
- New required fields
- Type changes (e.g., price_paid: number → string)
- Zod schema updates

## Migration

All consumers must update form inputs to match new schema.
