---
id: K-080
topic: hermes-url-trailing-slash
fact: The Hermes URL motor automatically appends a trailing slash to paths (e.g., /api results in /api/). This can break PostgREST queries if not sanitized before appending query parameters. The custom polyfill in polyfills.js handles this normalization.
valid_for:
  - mobile
  - networking
source: Session 2026-04-15 debugging
---

The Hermes URL motor automatically appends a trailing slash to paths (e.g., /api results in /api/). This can break PostgREST queries if not sanitized before appending query parameters. The custom polyfill in polyfills.js handles this normalization.