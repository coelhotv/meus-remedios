---
id: K-079
topic: hermes-url-recursion-trap
fact: In Hermes, the native 'href' getter calls toString() internally. Overriding toString() and accessing 'this.href' inside it causes infinite recursion and a RangeError. Always use a raw backing store or captured native method for property access inside overrides.
valid_for:
  - mobile
  - debugging
source: Session 2026-04-15 debugging
---

In Hermes, the native 'href' getter calls toString() internally. Overriding toString() and accessing 'this.href' inside it causes infinite recursion and a RangeError. Always use a raw backing store or captured native method for property access inside overrides.