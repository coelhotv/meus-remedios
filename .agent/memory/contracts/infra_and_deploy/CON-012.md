---
id: CON-012
name: cachedMedicineService (SWR wrapper)
file: src/shared/services/cachedServices.js
type: service-wrapper
consumers:
  - src/views/Medicines.jsx
  - src/views/Stock.jsx
  - src/features/dashboard/hooks/useDashboardContext.jsx
breaking_change_requires: ADR + cache invalidation strategy + consumer updates
status: stable
---

# CON-012: cachedMedicineService (SWR wrapper)

**File:** src/shared/services/cachedServices.js

## API

```javascript
getAll(): Promise<Medicine[]>  // Auto-cached, auto-invalidates on mutations
create(data): Promise<Medicine>  // Invalidates cache after create
update(id, data): Promise<Medicine>  // Invalidates cache after update
```

## Consumers

- `src/views/Medicines.jsx`
- `src/views/Stock.jsx`
- `src/features/dashboard/hooks/useDashboardContext.jsx`

## Breaking Changes

- Cache invalidation strategy change
- Return type change
- Auto-mutation behavior change
