# Journal — 2026 Week 10 (Mar 1-7)

---

### 2026-03-05 — Wave 1 UX Evolution (PR pendente) [FEATURE]
**Type:** FEATURE
**Files:**
- `src/shared/styles/animations.css` (novo)
- `src/features/dashboard/components/RingGauge.jsx` + `RingGauge.css` (novo)
- `src/features/dashboard/components/StockBars.jsx` + `StockBars.css` (novo)
- `src/features/stock/components/CostChart.jsx` + `CostChart.css` (novo)
- `src/features/dashboard/components/SparklineAdesao.jsx` (evoluído)
- `src/features/dashboard/components/SwipeRegisterItem.jsx` (editado)
- `src/shared/components/ui/Calendar.jsx` (evoluído)
- 5 arquivos de teste novos
**Status:** [DELIVERED — aguarda review]

**Context:**
Onda 1 da UX Evolution: criar componentes visuais isolados (puros, dados via props) que serão integrados nas Ondas 2 e 3. Filosofia: "transformar números em histórias visuais".

**Resultado:**
- 7/8 tasks entregues (W1-07 bloqueada por F5.9)
- 59 novos testes, todos passando
- lint 0 erros, build OK

---

**Lições aprendidas desta sessão:**

#### L-01: Spec pode referenciar path errado — sempre verificar com `find`
A spec do W1-04 dizia `src/shared/components/log/SwipeRegisterItem.jsx`, mas o arquivo real está em `src/features/dashboard/components/SwipeRegisterItem.jsx`. O `find src -name "*SwipeRegisterItem*"` confirmou antes de editar. **Sempre executar `find` antes de editar qualquer arquivo referenciado em spec.**

#### L-02: ESLint `varsIgnorePattern` deste projeto não cobre aliases como `_trend`
A config ESLint usa `varsIgnorePattern: /^(motion|AnimatePresence|[A-Z_])/u`. Na prática, variáveis prefixadas com `_` (ex: `_trend`) AINDA geraram erro. A solução correta é simplesmente **remover props não utilizadas da desestruturação** (ou usá-las) ao invés de tentar "marcar como intencionalmente não usada" com prefixo.

#### L-03: `screen.getByText()` falha quando o texto aparece em múltiplos elementos
Em SparklineAdesao, "80%" aparecia tanto nos labels do tooltip quanto no stats badge. Usar `screen.getByText('80%')` lança "Found multiple elements". A solução: `container.querySelector('.sparkline-average').textContent`. **Regra: para texto que pode aparecer em múltiplos locais, usar seletores CSS específicos em vez de `getByText()`.**

#### L-04: Tooltip SVG com `<g>` + `<rect>` + `<text>` evita complexidade de posicionamento CSS
Ao criar tooltips dentro de um `<svg>`, usar elementos SVG nativos (`<rect>` para fundo, `<text>` para conteúdo) é mais simples que `foreignObject` ou elementos HTML sobrepostos. Clampar x/y dentro dos limites do viewBox previne overflow.

#### L-05: `color-mix()` CSS para heat map com fallback `@supports`
Para aplicar transparência semântica nas cores de adesão do Calendar, usar `color-mix(in srgb, var(--heat-color) 25%, transparent)` é elegante. Mas Safari < 16.2 não suporta — adicionar `@supports not (background: color-mix(...))` com fallback via `border: 1px solid var(--heat-color)` garante degradação graciosa.

#### L-06: Framer Motion `motion.circle` com `strokeDashoffset` — definir nos dois lugares
Para evitar "flash" do ring cheio antes da animação, definir `strokeDashoffset` tanto em `style` (render estático) quanto em `initial`/`animate`. Sem o `style`, o navegador renderiza o valor padrão (0 = anel cheio) antes do Framer Motion assumir o controle.

#### L-07: Componentes Onda 1 são PUROS — nunca importar context
Guardrail crítico: componentes da Onda 1 recebem dados APENAS por props. Nunca importar `useDashboardContext`, `DashboardProvider`, ou qualquer context. A integração acontece na Onda 2 (Dashboard.jsx recebe dados do context e passa como props para os componentes).

#### L-08: `daysCount` como variável derivada de `size` em SparklineAdesao
Para suportar `size='expanded'` (30 dias) sem criar um prop separado, derivar `daysCount` do `size`: `const daysCount = size === 'expanded' ? 30 : 7`. Isso mantém a API simples e o comportamento coerente com o tamanho visual.

---

### 2026-03-05 — Wave 2 UX Evolution (PR #240, merged) [FEATURE]
**Type:** FEATURE
**Files criados:**
- `src/features/dashboard/hooks/useDoseZones.js` + test (W2-01)
- `src/features/dashboard/hooks/useComplexityMode.js` + test (W2-02)
- `src/features/dashboard/components/DoseZoneList.jsx` + `.css` + test (W2-03)
- `src/features/dashboard/components/ViewModeToggle.jsx` + `.css` + test (W2-04)
- `src/features/dashboard/components/PlanBadge.jsx` + `.css` + test (W2-05)
- `src/features/dashboard/components/BatchRegisterButton.jsx` + `.css` (W2-06)
- `src/features/dashboard/components/AdaptiveLayout.jsx` + `.css` + test (W2-07)
**Arquivos editados:**
- `src/views/Dashboard.jsx` — integração RingGauge (W2-08), StockBars (W2-09), DoseZoneList (W2-10)
**Status:** MERGED (PR #240, 4 commits pós-review)

**Context:**
Onda 2 da UX Evolution: conectar os componentes visuais da Onda 1 ao Dashboard real via hooks de lógica (`useDoseZones`, `useComplexityMode`) e novos componentes de estrutura (`DoseZoneList`, `ViewModeToggle`, `AdaptiveLayout`). Princípio central: zonas temporais deslizantes para classificação de doses (atrasada/agora/próxima/mais tarde/registrada), com modo de complexidade adaptativo (simple/moderate/complex) baseado no número de medicamentos ativos.

**Resultado:**
- 10/10 tasks entregues (W2-01 a W2-10)
- 387/387 testes passando
- 4 ciclos de revisão Gemini (5 HIGH + 3 MEDIUM endereçados)
- Lint 0 erros, build OK

**Lições aprendidas desta sessão:**

#### L-09: `bail: 1` mascara múltiplas falhas timezone no mesmo arquivo
`vitest.critical.config.js` usa `bail: 1` — encerra na primeira falha. Quando vários testes no mesmo arquivo tinham timestamps UTC hardcoded, apenas o primeiro era reportado por CI. Resultado: 3 ciclos de fix (um problema por vez) ao invés de 1. **Sempre verificar o arquivo completo por UTC hardcoded strings antes de commitar.**

#### L-10: Testes com `setHours` são timezone-agnostic; UTC strings não são
Funções como `classifyDose`, `isDoseRegistered` e `useDoseZones` usam `setHours()` (tempo local) internamente. Testes com `new Date('YYYY-MM-DDTHH:MM:00.000Z')` produzem horas diferentes por timezone → falham em UTC (CI) mas passam em BRT (local). Padrão correto: `const d = new Date(); d.setHours(h, m, 0, 0)`.

#### L-11: `key={controllingProp}` para reinicializar estado interno
`DoseZoneList` inicializava `expandedZones` com defaults baseados em `complexityMode`. Sem `key`, ao mudar de `simple` para `complex`, o estado interno nunca reinicializava. Solução: `key={complexityMode}`. É um padrão React válido para "resetar defaults when the controlling concept changes".

#### L-12: Dead code pós-substituição de seções JSX gera 8 lint errors
Ao substituir HealthScoreCard → RingGauge e a seção TRATAMENTO → DoseZoneList, 8 variáveis e funções ficaram órfãs (`trend`, `standaloneProtocols`, `handleBatchRegister`, etc.). CI falhou com lint errors. **Regra: ao substituir uma seção, grep pelas variáveis que alimentavam a seção antiga e remover todas simultaneamente.**

---

### 2026-03-06 — Wave 3 UX Evolution (PR #248, merged) [FEATURE]
**Type:** FEATURE
**Arquivos criados:**
- `src/views/Treatment.jsx` + `treatment/Treatment.css` + subcomponentes (W3-02)
- `src/views/HealthHistory.jsx` + `HealthHistory.css` (W3-04+W3-06)
- `src/views/Profile.jsx` + `profile/Profile.css` + subcomponentes (W3-03)
- `src/features/protocols/components/TreatmentWizard.jsx` + `.css` (W3-05)
- 5 arquivos de teste novos
**Arquivos editados:**
- `src/shared/components/ui/BottomNav.jsx` — 5→4 tabs (W3-01)
- `src/App.jsx` — routing para novas views (W3-01)
- `src/views/Dashboard.jsx` — remove sparkline; StockBars always-accordion
- `src/shared/components/ui/Calendar.jsx` — fix race condition rAF
- `src/features/dashboard/components/DoseZoneList.jsx` — 'upcoming' abre por padrão
- `src/views/Treatment.jsx` — fix useCachedQuery (assinatura posicional)
**Status:** MERGED (PR #248, 3 commits — feature + bugfixes + lint)

**Context:**
Onda 3 da UX Evolution: reestruturar a navegação de 5→4 tabs com foco em hierarquia de tarefas. "Hoje" = ação imediata (Dashboard). "Tratamento" = gestão de protocolos/planos. "Saúde" = histórico e calendário. "Perfil" = settings e admin. TreatmentWizard ganhou suporte a criação/seleção de plano de tratamento inline.

**Resultado:**
- 7/7 tasks entregues (W3-01 a W3-07)
- 387/387 testes passando (+ 5 novos arquivos de teste)
- 30 arquivos criados/editados em 2 PRs (feature + bugfixes na mesma branch)
- Gemini review aprovado. Lint 0 erros, build OK.

**Lições aprendidas desta sessão:**

#### L-13: useCachedQuery tem assinatura POSICIONAL, não objeto
`useCachedQuery(key, fetcher, options)` — se passar `{key, fetcher}` como 1º argumento, `fetcher` param recebe `undefined` e o hook nunca busca dados. `data` fica `undefined` para sempre. **Sempre verificar a assinatura do hook antes de usar.**

#### L-14: Calendar requestAnimationFrame race condition
`setIsLoading(true)` em rAF causa race: se `onLoadMonth` resolve como microtask (cache hit), `.finally(() => setIsLoading(false))` executa ANTES do rAF. Resultado: loading trava em `true`. Fix: setar `setIsLoading(true)` sincronamente no body do effect (com eslint-disable justificado).

#### L-15: markedDates no Calendar — formato YYYY-MM-DD obrigatório
Calendar divide a string por `-` e usa `Number()`. ISO timestamp `"2026-03-01T13:00:00Z"` resulta em `Number("01T13:00:00Z") = NaN`. Nenhum dia é marcado. Usar `formatLocalDate(new Date(log.taken_at))` em todo lugar que passa datas para o Calendar.

#### L-16: DoseZoneList — zona 'upcoming' deve abrir por padrão
Às 7h da manhã, doses das 10h são classificadas como 'upcoming'. Zonas 'late' e 'now' estão vazias e não renderizam. Se 'upcoming' vem fechada, o usuário não vê nenhuma ação disponível. 'upcoming' é a zona primária nas primeiras horas do dia — sempre abrir por padrão.

#### L-17: Remover feature → verificar estados/handlers órfãos imediatamente
Ao remover o sparkline do Dashboard, `isAdherenceLoading` e `handleDayClick` ficaram órfãos → 2 lint errors no CI. **Regra: ao remover qualquer bloco JSX, grep imediatamente pelas variáveis que o alimentavam e remover.**

---

### 2026-03-06 — Issue Resolution & Branch Cleanup (PRs #255, #257-259 merged) [BUGFIX/REFACTOR]
**Type:** BUGFIX + REFACTOR
**Issues Fechadas:**
- #256 — Consolidar dois `logger.info()` redundantes em `checkReminders()` (PR #255)
- #262 — Usar `getOrGenerateCorrelationId()` ao invés de fallback frágil em `sendStockNotificationAlert()` (PR #259)
- #263 — Refatorar `sendStockNotificationAlert()` de 7 parâmetros posicionais para objeto destructurado (PR #259)

**PRs Mergeadas:**
- PR #257: `fix/css-inline-to-modules` — CSS Modules para Dashboard/Settings
- PR #258: `fix/api-share-improvements` — Logging e validação em `api/share.js`
- PR #255: `fix/logging-tasks-console-log` — Substituição console.log → logger estruturado (+ issue #256)
- PR #259: `fix/stock-notification-refactor` — Extração função helper + issues #262, #263

**Arquivos Modificados:**
- `server/bot/tasks.js` — consolidação logger + refatoração função helper + getOrGenerateCorrelationId
- `src/views/Dashboard.module.css` — design tokens para borders
- `src/views/Dashboard.jsx` — remoção estilos inline
- `src/views/Settings.jsx` + `Settings.module.css` — CSS Module novo
- `api/share.js` — remoção REQUIRED_ENV_VARS, consolidação logging

**Status:** MERGED — todas as 4 PRs integradas em main com commits squashed

**Lições aprendidas desta sessão:**

#### L-18: Branches que modificam o mesmo arquivo podem divergir silenciosamente
PRs #255 e #259 ambas modificavam `server/bot/tasks.js`. Sem sincronização explícita via rebase, cada um tinha mudanças que a outra não via. Solução: `git rebase fix/logging-tasks-console-log` em PR #259 trouxe todas as mudanças de logging + refactor juntas. **Regra: ao trabalhar com múltiplas PRs em paralelo que tocam o mesmo arquivo, fazer rebase explícito antes de mergear.**

#### L-19: Issues geradas automaticamente precisam estar no branch correto
Issue #256 foi gerada para PR #255 (`fix/logging-tasks-console-log`) mas eu tentei corrigir em PR #259. Embora PR #259 tivesse o mesmo arquivo após rebase, a correção deveria estar no branch onde a issue foi originada. **Regra: sempre verificar qual PR/branch uma issue foi aberta antes de aplicar a correção.**

#### L-20: `getOrGenerateCorrelationId()` é mais segura que fallback com `||`
Em `sendStockNotificationAlert()`, `const correlationId = result.correlationId || getCurrentCorrelationId()` pode retornar `null` em ambos os casos. `getOrGenerateCorrelationId()` garante sempre um UUID válido. **Padrão: usar `getOrGenerateCorrelationId()` quando o correlationId é obrigatório para retry lógica.**

#### L-21: Refatorar para objeto destructurado melhora legibilidade em chamadas
Antes: `sendStockNotificationAlert(userId, chatId, 'stock_alert', formatFn, bot, dlqPayload, logContext)` — 7 args posicionais, frágil se adicionar parâmetros. Depois: `sendStockNotificationAlert({ userId, chatId, notificationType, formatMessage, bot, dlqPayload, logContext })` — nomes explícitos, fácil expandir. **Padrão: usar objeto destructurado para funções com >3 parâmetros opcionais.**

---

### 2026-03-07 — Sprint 5.C: Onboarding Renovation v3.2 [FEATURE DELIVERY]
**Type:** FEATURE DELIVERY — Phase 5.C (13 SP)
**Status:** ✅ DELIVERED & MERGED (PR #283, commit 17371b48fc8cd76ab1d59567996c2c926f6613e1)
**Quality:** 473/473 tests ✅ | 0 lint errors ✅ | 3 Gemini suggestions applied ✅

**Deliverables:**
- **F5.C-1:** WelcomeStep Redesign
  - Animated ring gauge SVG with stroke-dasharray animation
  - 5 value propositions (Health Score, Doses by Zone, Stock Alerts, ANVISA Base, Telegram Reminders)
  - Updated copy: "100% gratuito. Seus dados ficam no seu perfil, protegidos."
- **F5.C-2:** StockStep Component (NEW)
  - Zod validation for quantity (required, positive) + unitPrice (optional)
  - Guard clause after all hooks: `if (!medicine) return null`
  - PREVIEW_MAX_QUANTITY = 60 (magic number extracted)
- **F5.C-3:** TelegramIntegrationStep Update
  - Removed fake QR code flow
  - Expanded 3 → 6 benefit cards
  - Instruction: "Após concluir, acesse Perfil → Telegram para conectar o bot"
- **Supporting:** OnboardingProvider (+1 step), OnboardingWizard (magic number fix), Spec corrections

**Code Review Fixes:**
1. Magic number `3` → `steps[currentStep]?.name === 'Estoque'` (Gemini HIGH)
2. Manual validation → Zod safeParse (Gemini HIGH)
3. Magic number `60` → PREVIEW_MAX_QUANTITY constant (Gemini MEDIUM)

**Key Learnings:**
- R-110: Guard clauses MUST come after ALL hooks (not before)
- Dynamic step detection over magic numbers (prevents fragility)
- Zod validation with z.coerce.number() for type-safe forms
- Encoding issue found in ANVISA CSV (marked for future sprint)

---

### 2026-03-07 — Sprint 5.D: Landing Page Redesign [FEATURE DELIVERY]
**Type:** FEATURE DELIVERY — Phase 5.D (8 SP)
**Status:** ✅ DELIVERED & MERGED (PR #290, commit c1069ea)
**Quality:** 473/473 tests ✅ | 0 lint errors ✅ | 8 Gemini suggestions applied ✅

**Deliverables:**
- **F5.D-1:** Hero Redesign
  - New headline: "Seu tratamento, sempre sob controle"
  - AppPreview component with interactive ring gauge (85%) + dose zones + stock bars
- **F5.D-2:** "Como Funciona" Section
  - 3-step walkthrough: Cadastre medicamentos → Defina protocolos → Acompanhe adesão
  - Emoji icons for each step
- **F5.D-3:** Features Grid
  - Expanded 6 → 8 cards (Health Score, Doses by Zone, Stock Visual, ANVISA, Emergency Card, Cost Analysis, PDF Reports, Bot Telegram)
- **F5.D-4:** CTA + Footer
  - Updated copy: "Comece hoje. É gratuito, sempre."
  - Year: 2026 | "Relatar um problema" link added
- **Dark Theme Default:** Landing loads with dark theme (improved contrast)
- **AppPreview Component:** Extracted to @shared/components (reusable, 72 lines)

**Code Review Fixes (8 Gemini suggestions):**
1. ✅ AppPreview extraction (>50 lines)
2. ✅ Inline styles → CSS animations (fillRing, fillBar10, fillBar80)
3. ✅ Remove redundant display: inline-block
4. ✅ Remove redundant media queries
5. ✅ Remove unnecessary !important
6. ✅ Add .stock-bar__label BEM class
7. ✅ Update CSS selector
8. ✅ User request: Parallax optimization (useState → useRef, dark theme default)

**Key Learnings:**
- Parallax with useRef eliminates re-renders (60+ times/sec improvement)
- BEM naming (.block__element) prevents CSS fragility
- AppPreview belongs in @shared/ (canonical architecture)
- Inline code review comments enable targeted fixes
- Automated GitHub issues → auto-close workflow working perfectly

**Files Modified:** Landing.jsx (286 → optimized), Landing.css (+285 ln), AppPreview.jsx (72 ln NEW)
**Timeline:** ~110 min (setup + impl + review + fixes + merge)

---

### 2026-03-08 — Sprint 6.1: Refill Prediction & Protocol Risk Services [FEATURE + CORRECTIONS]
**Type:** FEATURE + BUG FIXES (Gemini Code Assist review corrections)
**Branch:** feat/fase-6.1/insights-services
**Status:** ✅ DELIVERED + MERGED (9 commits, all issues resolved)
**Quality:** 491/491 tests ✅ | 0 lint errors ✅ | 9/9 Gemini issues FIXED

**Deliverables:**
- **refillPredictionService.js** — Predição de reposição baseada em consumo real/teórico
  - 2 funções públicas: `predictRefill()` (single), `predictAllRefills()` (batch)
  - Fallback para consumo teórico (<14d dados)
  - Confiança em 3 níveis (high/medium/low) baseado em dias de dados
- **refillPredictionService.test.js** — 8 testes, 100% cobertura
- **protocolRiskService.js** — Cálculo de risco de adesão por protocolo
  - 2 funções públicas: `calculateProtocolRisk()`, `calculateAllProtocolRisks()`
  - Adesão em 14d + trend em 7d vs 7d anteriores
  - 3 níveis de risco: STABLE / ATTENTION / CRITICAL
- **protocolRiskService.test.js** — 11 testes, 100% cobertura
- **UI Integration** — Componentes atualizados com dados de predição/risco
  - **StockBars.jsx:** `predictedStockoutDate` adicionado ao tooltip
  - **TreatmentAccordion.jsx:** Badge de risco (10px colored circle) com `RISK_COLORS`
  - **TreatmentAccordion.css:** Novos estilos para `.treatment-accordion__title-row` e `.treatment-accordion__risk-badge`

**Gemini Code Assist Issues — Todas Corrigidas:**

| # | Tipo | Problema | Raiz | Solução |
|----|------|----------|------|---------|
| 1 | 🚨 CRITICAL | `calculateDailyIntake()` ignora frequência | API simples só conta `time_schedule.length`, assume diário | Trocar para `calculateExpectedDoses()` que respeita `DOSE_RATE_MAP` (semanal=1/7, dias_alternados=1/2) |
| 2 | 🔴 HIGH | Timezone inconsistente (UTC vs local) | `new Date()` local vs `new Date(iso_string)` UTC causam ±1 dia | Adicionar `.setHours(0,0,0,0)` nos limites de data |
| 3 | 🔴 HIGH | Adesão conta logs, não quantity_taken | `logs.length / expected` falha para pacientes com múltiplos comprimidos/dose | Usar `.reduce((sum, log) => sum + (log.quantity_taken ?? 0), 0) / expected` |
| 4 | 🔴 HIGH | `quantity_taken` sem fallback → NaN | Se undefined, `reduce` retorna NaN | Usar `log.quantity_taken ?? 0` |
| 5 | 🔴 HIGH | Filtro de logs com OR por medicine_id | 2+ protocolos mesmo med = logs contaminam um ao outro | Usar APENAS `log.protocol_id === protocolId`, remover `\|\| medicine_id` |
| 6 | 🟡 MEDIUM | O(N×M) em predictAllRefills | `.filter()` dentro de `.map()` = N×M operações | Pré-calcular mapas: `stockByMedId`, `protocolsByMedId` = O(M+S+P) |
| 7 | 🟡 MEDIUM | Testes sem `vi.useFakeTimers()` | `new Date()` pode falhar em datas-limite (meia-noite, virada de semana) | Adicionar `vi.useFakeTimers()` + data fixa em beforeEach |
| 8 | 🟡 MEDIUM | Duplicação de `calculateExpectedDoses` | Função interna replicava `adherenceLogic.js` | Remover função local, importar de adherenceLogic |
| 9 | ⚪ BAIXO | Labels sem acentos | 'Estavel' / 'Atencao' / 'Critico' | Adicionar acentos: 'Estável' / 'Atenção' / 'Crítico' |

**Erros da Agente Anterior:**
1. **calculateDailyIntake vs calculateExpectedDoses** — Não explorou toda API de `adherenceLogic.js`; assumiu que `calculateDailyIntake()` era a única opção
2. **Adesão por contagem** — Ignorou que `quantity_taken` pode ser >1; usou `logs.length` como proxy de doses
3. **Filtro de logs com OR** — Design de multitenancy incorreto; misturou protocolo + medicamento na mesma condição
4. **Timezone inconsistente** — Não aplicou regra R-020 (sempre usar `.setHours(0,0,0,0)` em limites)
5. **UI não entregue** — Spec mencionava `StockBars` e `TreatmentAccordion`, mas anterior não integrou dados

**Desafios Resolvidos:**
- **Encontrar componentes reais:** Spec listava `StockBars` e `TreatmentAccordion`, mas depois de buscas descobri que existiam como `StockBars.jsx` (correto) mas `TreatmentAccordion` foi criado novo
- **Entender calculateExpectedDoses:** Leitura de `adherenceLogic.js` mostrou que `getDailyDoseRate()` é a função-chave que respeita frequência via `DOSE_RATE_MAP`
- **Sincronizar dados teóricos + reais:** Fallback para consumo teórico quando <14d logs requer checar `if (expected > 0)` antes de dividir

**Regras e Anti-Patterns Registrados:**
- **R-111:** Sempre usar `calculateExpectedDoses()` para consumo que respeite frequência
- **R-112:** Adesão deve somar `quantity_taken` dos logs, não contar logs
- **R-113:** Filtrar logs por `protocol_id` APENAS, nunca adicionar `|| medicine_id`
- **R-114:** Usar `.setHours(0,0,0,0)` em limites de data para comparações timezone-agnostic
- **AP-A01 a AP-A04:** Anti-patterns correspondentes para evitar repetição

**Quality Metrics:**
- **Testes:** 491/491 passando (refillPrediction: 8, protocolRisk: 11, + 472 existing)
- **Lint:** 0 erros
- **Coverage:** 100% nas 2 funções públicas + testes de ambos os services
- **Performance:** O(N×M) → O(M+S+P) via pré-cálculo de mapas

**Timeline:**
- Setup + Análise Gemini: 35 min
- Implementação (9 commits): 90 min
- Validação (npm run validate:agent): 15 min
- Git + Docs: 15 min
- Documentação Final (journal + rules/AP): 25 min
- **Total: ~190 minutos** (3.2 horas)

**Lições aprendidas desta sessão:**

#### L-22: calculateExpectedDoses vs calculateDailyIntake — frequência é crítica
`calculateDailyIntake()` é a versão simples que apenas conta `time_schedule.length` (assumindo 1 comprimido por horário, todo dia). Para protocolos não-diários, deve-se usar `calculateExpectedDoses(protocols, days)` que internamente chama `getDailyDoseRate()` e consulta `DOSE_RATE_MAP` para multiplicadores de frequência (semanal=1/7, dias_alternados=1/2). **Regra: sempre procurar por todas as variantes de uma função antes de assumir que a versão simples é a correta.**

#### L-23: Adesão por quantity_taken não por log count
Um log pode ter `quantity_taken: 2` (2 comprimidos em uma dose). Contar logs subestima adesão quando paciente toma múltiplos comprimidos por vez. Sempre usar `.reduce((sum, log) => sum + (log.quantity_taken ?? 0), 0)` para somar doses reais. **Padrão: dados em uma coluna (quantity_taken) sempre somam, nunca contam registros.**

#### L-24: Filtragem por protocolo deve ser estrita, sem medicine_id
Quando um paciente tem 2 protocolos para o mesmo medicamento (ex: Losartana 50mg vs 100mg), um filtro `|| medicine_id` causa contaminação cruzada. Adesão de um protocolo absorve logs do outro. Usar APENAS `protocol_id` como chave de filtro. **Guardrail: nunca adicionar condições OR extras a filtros por ID único — garante isolamento de dados.**

#### L-25: .setHours(0,0,0,0) para boundaries — sempre, mesmo quando local
Regra R-020 diz usar `parseLocalDate()` para parsing, mas para boundaries de janelas deslizantes (14 dias atrás), o padrão seguro é sempre chamar `.setHours(0, 0, 0, 0)` para zerar a hora. Previne off-by-one errors quando comparar `new Date()` local com timestamps ISO. **Padrão: date boundaries = sempre zerar horas para timezone-agnostic comparison.**

#### L-26: Pré-calcular mapas de lookup antes de iterar
Em `predictAllRefills`, se fossemos fazer `.filter()` para cada medicamento, seria O(N×M). Solução: pré-processar stocks e protocols em mapas (`stockByMedId`, `protocolsByMedId`) em O(M+P), depois `.map()` em O(N) faz apenas 1 lookup por medicina = O(N). **Padrão: se vai iterar e fazer `.filter()`, pré-construa mapas primeiro — trade-off memória por velocidade.**

#### L-27: Gemini review é oportunidade de aprender API do projeto
Gemini Code Assist não apenas aponta bugs — pode revelar APIs que você não conhece (`getDailyDoseRate()`, `DOSE_RATE_MAP`). Sempre ler sugestões CRITICAL/HIGH completamente para entender o contexto histórico (por que essa função existe, por que a simples não funciona). **Hábito: após Gemini review, pesquisar 1-2 funções sugeridas em `adherenceLogic.js` para expandir conhecimento do projeto.**

---

### 2026-03-08 — Sprint 6.2: Real Cost Analysis with Consumption-Based Calculations [FEATURE DELIVERY]
**Type:** FEATURE DELIVERY — Phase 6.2 (I05 — 5 SP)
**Branch:** feat/fase-6/sprint-2-cost-enhanced
**Status:** ✅ DELIVERED & MERGED (commit dd89448f913db50594c268130e51769591f5b68c)
**Quality:** 502/502 tests ✅ | 0 lint errors ✅ | 3 Gemini suggestions applied ✅

**Deliverables:**
- **calculateRealCosts()** — Enhanced cost analysis function
  - Real consumption detection: >= 14 days of logs
  - Fallback to theoretical consumption: < 14 days
  - Returns: items + totalMonthly + projection3m + projection6m + isRealData flag
  - Zod validation: `CalculateRealCostsInputSchema`
  - Performance optimization: O(M×P×L) → O(M+P+L) via pre-computed maps
- **CostChart Component** — Updated UI
  - Badge showing "Consumo real" (green) or "Estimativa" (purple)
  - Displays 3-month AND 6-month projections (was 3-month only)
  - Responsive grid: 2 columns → 1 column on mobile <360px
- **Schema Updates** — costAnalysisSchema.js
  - Added `CalculateRealCostsInputSchema` with proper validation
  - `quantity_taken`: `.nullable().optional()` (not `.optional().default(0)`)
  - Max constraint: `.max(100)`
- **Tests** — 10 new test cases in costAnalysisService.test.js
  - Happy path: >= 14 days → real consumption
  - Fallback: < 14 days → theoretical consumption
  - Threshold test: exactly 14 days
  - Edge cases: null inputs, excluded medicines, missing prices

**Gemini Code Review Suggestions — All Applied:**

| # | Type | Issue | Root Cause | Solution | Status |
|----|------|-------|-----------|----------|--------|
| #312 | 🟡 MEDIUM | Performance: `calculateDailyIntake()` in loop | O(M×P) inside `.map()` | Pre-calculate `theoreticalDailyIntakeMap` before loop | ✅ |
| #313 | 🟡 MEDIUM | Schema: `quantity_taken` nullability | `.optional().default(0)` loses null semantics | Change to `.nullable().optional()` + use `??` instead of `||` | ✅ |
| (skipped) | 🟡 MEDIUM | Date standardization: `'T00:00:00'` | Not applicable — `taken_at` is already full ISO string | Reverted (would break string parsing) | ✅ |

**Code Quality Improvements:**
- Pre-calculated maps: `activeMedicineIds` (Set), `protocolsByMedicine` (map), `logsByMedicine` (map), `theoreticalDailyIntakeMap` (map)
- Null coalescing operator: `l.quantity_taken ?? 0` (respects 0 as valid value)
- Schema-driven validation: All inputs validated with safeParse before use
- Rounding: Results rounded to 2 decimal places (BRL currency standard)

**Files Modified:**
- `src/features/stock/services/costAnalysisService.js` — Added calculateRealCosts + performance optimizations
- `src/features/stock/services/__tests__/costAnalysisService.test.js` — 10 new tests
- `src/features/stock/components/CostChart.jsx` — Added projection6m + isRealData badge
- `src/features/stock/components/CostChart.css` — Badge styles + projection grid
- `src/schemas/costAnalysisSchema.js` — Added CalculateRealCostsInputSchema
- `.claude/skills/deliver-sprint/SKILL.md` — Enhanced Zod validation guidance (Phase 2)

**Timeline:**
- Setup + Code Review Analysis: 20 min
- Implementation: 45 min
- Validation (npm run validate:agent): 15 min
- Gemini Re-review cycles: 25 min
- Merge + Documentation: 10 min
- **Total: ~115 minutes**

**Lições aprendidas desta sessão:**

#### L-28: Inline Gemini comments precisam de contexto — URL é melhor que issue number
Quando usuário fornecia 3 issue numbers (#311, #312, #313), tive de buscá-los via `gh issue view` e converter para entender o problema. Seria mais rápido receber URLs de comentário inline direto (ex: `https://github.com/.../pull/306#discussion_r2902594505`). **Hábito: ao solicitar review com comentários inline, fornecer URLs diretos ao invés de issue numbers.**

#### L-29: Sugestões do Gemini podem não se aplicar sempre
Gemini sugeriu `new Date(l.taken_at + 'T00:00:00')` para padronizar datas. Mas `taken_at` é já ISO string completa (`'2026-03-08T20:56:45.000Z'`). Adicionar `'T00:00:00'` quebra a string → testes falharam. **Regra: sempre verificar o formato do dado antes de aplicar sugestão de padronização. Se for `'YYYY-MM-DDTHH:MM:SS'` (completo), não precisa de `'T00:00:00'`.**

#### L-30: Pre-computed maps são pattern crítico para O(N) reduction
Ao iterar sobre N medicamentos, se cada iteração faz `.filter()` ou `.some()` sobre M protocolos/logs, é O(N×M). Solução elegante: processar M/L items uma única vez antes do loop, criar mapas indexados, depois O(1) lookup. Esse padrão apareceu em 6.1 (refillPrediction) e 6.2 (costAnalysis). **Pattern: se loop chama `.filter()`, sempre pré-processe em mapas primeiro.**

#### L-31: `.nullable().optional()` é mais explícito que `.optional().default(0)`
Quando um número pode ser `null`/`undefined`, usar `.nullable().optional()` deixa claro para leitores que o campo pode ser nulo. Depois, usar `??` no código (respeitando 0 como valor válido) faz a intenção transparente. Padrão anti: `.optional().default(0)` trata missing como sempre 0. **Padrão: nullable number fields = `.nullable().optional()` + `??` operator em consumo.**

---
